global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "fastapi-app"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["app-blue:8000", "app-green:8000"]
